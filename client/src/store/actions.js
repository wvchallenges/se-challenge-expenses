/**
 * upload-expense-report
 *
 * This action expects to be invoked with an object containing a `file` key whose value is a `File`
 * which the user has selected to upload.
 *
 * # Data
 *
 * ```
 * {
 *   file: File,
 * }
 * ```
 */
export const UPLOAD_EXPENSE_REPORT = 'upload-expense-report'
