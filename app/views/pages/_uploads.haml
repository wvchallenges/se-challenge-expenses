%table.table.table-striped.table-sm
  %thead
    %tr
      %th ID
      %th Original file name
      %th Uploaded At

  %tbody
    - uploads.each do |upload|
      %tr
        %td= upload.id
        %td= link_to upload.file_name, { upload_id: upload.id }
        %td= upload.uploaded_at
