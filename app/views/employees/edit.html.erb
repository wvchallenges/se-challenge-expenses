<% provide(:title, "Edit Employee Info: " + @employee.name) %>
<% provide(:button_text, 'Update Employee Info') %>
<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>