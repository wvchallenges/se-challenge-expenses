= form_tag upload_expense_sheets_path, multipart: true do
  .fields
    = file_field_tag :file, accept: 'text/csv'
  %br
  .actions
    = submit_tag "Upload", class: 'btn btn-large btn-success'
    or
    = link_to 'Cancel', expense_sheets_path
