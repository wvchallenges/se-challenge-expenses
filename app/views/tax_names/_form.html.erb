<%= form_for(@tax_name) do |f| %>
  <%= render 'shared/error_messages_tax_name', object: @tax_name %>
  
  <%= f.label :name %>
  <%= f.text_field :name, class: 'form-control' %>

  <%= f.submit yield(:button_text), class: "btn btn-primary" %>
<% end %>